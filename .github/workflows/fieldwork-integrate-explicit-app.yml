name: Fieldwork integrate explicit app precedence (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Superseded by artifact review: explicit --app is not a complete stub-suffix escape for this bug fix.'
          echo 'See Linux Fieldwork APP_PROVENANCE_EXPERIMENT.md and issue #490.'
