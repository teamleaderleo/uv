name: Fieldwork unit 02 isolated materialization (superseded)

on:
  workflow_dispatch:

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Superseded by fieldwork-unit-02-last-mile.yml'
